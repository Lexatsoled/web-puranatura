# Local development env sample (DO NOT COMMIT secrets)
# Copy to .env.local and fill values when needed

VITE_API_URL=http://localhost:3001
VITE_ENABLE_ANALYTICS=true
VITE_GA_ID=G-XXXXX
VITE_FB_PIXEL_ID=FB-XXXXX


# Backend secrets
# LLM provider keys retired: do NOT store provider API keys in repo.
# If you need to integrate an LLM provider locally, configure the key in
# your local .env file or secret manager (do not commit).

## Example secret placeholders (do NOT store secrets in the repo)
# JWT_SECRET=
# JWT_REFRESH_SECRET=
DATABASE_URL=file:./backend/database.sqlite
SENTRY_DSN=
# REDIS_PASSWORD=
# BACKUP_ENCRYPTION_KEY=
# OPENAI_API_KEY removed from samples (project avoids embedded provider keys)
