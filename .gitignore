# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Archivos de entorno (no versionar secretos)
.env
.env.*
!.env.example
!.env.production.example
backend/.env
backend/.env.*
!backend/.env.example

# Database files and backups (no versionar DB)
backend/*.sqlite
backend/*.sqlite-*
backend/backups/
backend/out.log
backend/logs/
# ignore prisma test dbs and local generated DB files inside prisma
backend/prisma/**/*.sqlite
backend/prisma/**/test-*.sqlite
# ignore sqlite journal/temp files
**/*.sqlite-journal
# Artefactos y binarios locales
.cache/
reports/tmp/
# Ignore all reports by default (we generate them in CI and archive as artifacts)
reports/
# Keep axe-report.json tracked because we rely on it for local QA
!reports/axe-report.json
.tmp-lh/
tmp-lh/
.lighthouse-temp/
backend/prisma/prisma/dev.db
backend/prisma/*.db
data/products.js
data/products.js.map
Secretos/
backups/
localhost_*.report.html
reports/observability/

# Transient and coverage artifacts we don't want in the repo
coverage/
tmp/
**/dev.db*
**/*.db
**/*.db
**/*.sqlite
tmp-*.cjs
tmp-*.js
*.sql
tmpfile
